// Dummy test to show it works.
describe('A test working', () => {
  it('can add 1 + 1', () => {
    console.log('log');
    console.warn('warn');
    console.error('error');
    expect(1 + 1).toEqual(2);
  });
});
